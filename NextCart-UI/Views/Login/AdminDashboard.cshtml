<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NextCart Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        /* Custom styles to enhance aesthetics and compactness (keep your existing styles) */
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(to bottom right, #eef2f3, #e0e6eb);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: stretch;
            padding-top: 2rem;
        }

        .main-content-area {
            flex-grow: 1;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 1rem;
        }

        .container {
            background-color: #ffffff;
            border-radius: 1rem;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.08);
            padding: 2rem;
            width: 100%;
            max-width: 960px;
        }

        h2 {
            color: #1f2937;
            font-weight: 800;
            font-size: 2.25rem;
            margin-bottom: 1rem;
            text-shadow: 1px 1px 1px rgba(0,0,0,0.03);
        }

        hr {
            margin-top: 1rem;
            margin-bottom: 1.5rem;
            border-top: 2px solid rgba(0, 0, 0, 0.05);
        }

        .dashboard-button {
            padding: 0.8rem 1.5rem;
            font-weight: 700;
            border-radius: 0.5rem;
            box-shadow: 0 5px 10px rgba(0,0,0,0.1);
            transition: all 0.2s ease-in-out;
            letter-spacing: 0.01em;
            font-size: 1.1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

            .dashboard-button svg {
                width: 1.5rem;
                height: 1.5rem;
                margin-right: 0.5rem;
            }

            .dashboard-button:hover {
                transform: translateY(-0.125rem);
                box-shadow: 0 8px 16px rgba(0,0,0,0.15);
            }

        /* Specific button colors (using Bootstrap's theming and custom override) */
        .btn-custom-blue {
            background-color: #2563eb;
            color: #ffffff;
            border-color: #2563eb;
        }

            .btn-custom-blue:hover {
                background-color: #1d4ed8;
                border-color: #1d4ed8;
            }

            .btn-custom-blue:focus {
                box-shadow: 0 0 0 0.25rem rgba(37, 99, 235, 0.5);
            }

        .btn-custom-teal {
            background-color: #0d9488;
            color: #ffffff;
            border-color: #0d9488;
        }

            .btn-custom-teal:hover {
                background-color: #0f766e;
                border-color: #0f766e;
            }

            .btn-custom-teal:focus {
                box-shadow: 0 0 0 0.25rem rgba(13, 148, 136, 0.5);
            }

        .btn-custom-rose {
            background-color: #e11d48;
            color: #ffffff;
            border-color: #e11d48;
        }

            .btn-custom-rose:hover {
                background-color: #be123c;
                border-color: #be123c;
            }

            .btn-custom-rose:focus {
                box-shadow: 0 0 0 0.25rem rgba(225, 29, 72, 0.5);
            }

        /* User Role Indicator */
        .user-role-indicator {
            font-size: 1rem;
            font-weight: 600;
            color: #495057;
            background-color: #e9ecef;
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
            margin-bottom: 1.5rem;
            text-align: center;
            border: 1px solid #dee2e6;
        }

            .user-role-indicator.admin {
                background-color: #dc3545;
                color: #ffffff;
                border-color: #dc3545;
            }

            .user-role-indicator.user {
                background-color: #28a745;
                color: #ffffff;
                border-color: #28a745;
            }

        /* Footer styling */
        .app-footer {
            width: 100%;
            background-color: #343a40;
            color: #f8f9fa;
            padding: 1rem 0;
            text-align: center;
            font-size: 0.8rem;
            box-shadow: 0 -3px 10px rgba(0,0,0,0.1);
            margin-top: auto;
        }
    </style>
</head>
<body class="bg-light">
    <div class="main-content-area">
        <div class="container">
            <h2 class="text-center mb-4">NextCart Admin Dashboard</h2>
            <hr class="mb-4">

            <div id="userRoleIndicator" class="user-role-indicator mx-auto" style="max-width: 300px;">
            </div>

            <div class="d-flex flex-column flex-md-row justify-content-center gap-3 mb-4">
                <button id="showCustomerListBtn" class="dashboard-button btn-custom-blue">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 me-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17 20h-2V10h2m4 0h-2V20h2M5 20h-2V10h2m4 0h-2V20h2M12 4v16m-4-8h8m-4 0V4" />
                    </svg>
                    Registered Customer List
                </button>
                <button id="goToProductsBtn" class="dashboard-button btn-custom-teal">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 me-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                    </svg>
                    Product List
                </button>
                
                <button id="goToUserReportBtn" class="dashboard-button btn-custom-rose">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 me-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M3 3h18M3 7h18M3 11h18M3 15h18M3 19h18" />
                    </svg>
                    User Report Page
                </button>
            </div>

        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // No longer need customerListSection, customerCardsContainer, noCustomersAlert
            // const customerListSection = document.getElementById('customerListSection');
            const showCustomerListBtn = document.getElementById('showCustomerListBtn');
            const goToProductsBtn = document.getElementById('goToProductsBtn');
            const goToPaymentAdminBtn = document.getElementById('goToPaymentAdminBtn');
            // const customerCardsContainer = document.getElementById('customerCardsContainer');
            // const noCustomersAlert = document.getElementById('noCustomersAlert');
            const userRoleIndicator = document.getElementById('userRoleIndicator');

            // No longer need to fetch customers here
            // const customers = @Html.Raw(Json.Serialize(Model));

            const currentUserRole = "@ViewBag.CurrentUserRole";

            // Function to render customer cards (no longer needed here, but kept for context if you had other uses)
            // function renderCustomerCards() { ... }

            // Function to display the user role (keep this)
            function displayUserRole() {
                if (currentUserRole) {
                    userRoleIndicator.textContent = `Logged in as: ${currentUserRole}`;
                    if (currentUserRole.toLowerCase() === 'admin') {
                        userRoleIndicator.classList.add('admin');
                        userRoleIndicator.classList.remove('user');
                    } else {
                        userRoleIndicator.classList.add('user');
                        userRoleIndicator.classList.remove('admin');
                    }
                } else {
                    userRoleIndicator.textContent = 'Logged in as: Guest';
                    userRoleIndicator.classList.remove('admin', 'user');
                }
            }

            // Call displayUserRole on page load
            displayUserRole();

            // Event listener for "Registered Customer List" button
            showCustomerListBtn.addEventListener('click', function() {
                // Redirect to the new CustomerList page
                window.location.href = '@Url.Action("RegisteredUsers", "Login")'; // Assuming AdminController
            });

            // Event listener for "Product List" button
            goToProductsBtn.addEventListener('click', function() {
                window.location.href = '@Url.Action("Index", "Product")';
            });

            // Event listener for "Payment Admin" button
            // goToPaymentAdminBtn.addEventListener('click', function() {
            //     alert("Navigating to Payment Admin (e.g., /Admin/PaymentAdmin)");
            //     console.log("Simulating navigation to Payment Admin. In a real app, this would be a redirect to a Payment Admin action.");
            // });
            const goToUserReportBtn = document.getElementById('goToUserReportBtn');
            goToUserReportBtn.addEventListener('click', function () {
                window.location.href = '@Url.Action("UserReport", "User")';
            });
        });
    </script>
</body>
</html>